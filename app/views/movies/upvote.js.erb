$('#like-<%= @movie.id %>').bind('ajax:success', function() {
   $('#up-<%= @movie.id %>').html('<%=escape_javascript (@movie.get_upvotes.size + @movie.like).to_s %>');
   $('#down-<%= @movie.id %>').html('<%=escape_javascript (@movie.get_downvotes.size + @movie.dislike).to_s %>');
});